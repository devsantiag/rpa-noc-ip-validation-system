
// responsável por registrar no HTML e processar no que concerne a validação de não compatibilidade
// caso seja compatível com um IP já registrano no sistema, retornará ao usuário um alerta (em texto e circular nos elementos) e o mesmo o rejeitará


function IpValidationProcess() {
    
    return (
        <div>

            <input 
            type="text" 
            name="inputIp" 
            id="inputIpMaquinaRpa" />

        </div>
    )

}
export default IpValidationProcess;